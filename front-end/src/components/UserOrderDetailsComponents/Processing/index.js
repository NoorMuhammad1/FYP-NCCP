import React from "react";

const Processing = (props) => {
  return (
    <>
      The order is currently in the processing phase. It will take about 2 to 3
      days for it to process. Wait until it changes.
    </>
  );
};

export default Processing;
